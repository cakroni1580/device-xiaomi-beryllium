<?xml version="1.0" encoding="UTF-8"?>
<manifest>

  <!-- ========================= -->
  <!-- Original Repositories     -->
  <!-- ========================= -->

  <remote name="aospm" fetch="https://github.com/cakroni1580" />

  <project path="device/generic/sdm845"
           name="android_device_generic_sdm845"
           revision="main"
           remote="aospm"
           groups="default"
           replace="true" />

  <project path="external/tinyhal"
           name="tinyhal"
           revision="master"
           remote="aospm"
           groups="default"
           replace="true" />

  <project path="external/vibrator-ff"
           name="external_vibrator-ff"
           revision="main"
           remote="aospm"
           groups="default"
           replace="true" />


  <!-- ========================= -->
  <remove-project name="device/google" />
  <remove-project name="kernel/google" />
  <remove-project name="platform/prebuilts" />
  <remove-project name="platform/hardware/google" />
  <!-- ========================= -->

  <!-- System core (init, ueventd, etc) -->
  
  <!-- mkbootimg (required for vendor_boot & dtbo) -->

  <!-- Clang prebuilt -->
  
  <!-- Prebuilt GKI kernels folder -->
  

</manifest>
